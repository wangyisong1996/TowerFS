TowerGame
	TowerConfig
		init_HP
		init_ATK
		init_DEF
		init_GOLD
		
		n_monsters
		monsters
			name
			short_name
			HP
			ATK
			DEF
			GOLD
		monster_map
		
		n_nodes
		nodes
			name
			event1
				type
				n_monsters
				monsters(string)
			event2
				...
			n_link
			links
				type
				name
		node_map
		
	TowerStatus
		config(TowerConfig)
		nodes(NodeStatus)
			config(NodeConfig)
				event1(NodeEventStatus)
					config(NodeEventConfig)
						type
						n_monsters
						monsters(string)
					tower_config(TowerConfig)
						...
					player(PlayerStatus)
						config(TowerConfig)
						HP
						ATK
						DEF
						GOLD
						fight_log
				event2(NodeEventStatus)
					...
			tower_config(TowerConfig)
				...
			player(PlayerStatus)
				...
			name
			event1(NodeEventStatus)
				...
			event2
				...
			links
		node_map
		readme_file
		fight_file
